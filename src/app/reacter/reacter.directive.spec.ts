import { ReacterDirective } from './reacter.directive';

describe('ReacterDirective', () => {
  it('should create an instance', () => {
    const directive = new ReacterDirective();
    expect(directive).toBeTruthy();
  });
});
